import { createSlice } from "@reduxjs/toolkit";

const newOrderFormSlice = createSlice({
   name: "newOrder",
   initialState: {},
   reducers: {},
   extraReducers: {},
});

export default newOrderFormSlice.reducer;
